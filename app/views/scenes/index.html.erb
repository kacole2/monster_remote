<% provide(:title, 'Settings') %>
<!DOCTYPE html>
<html>
  <head>
    <title><% :title %>	</title>
  </head>

<h2>MonsterShield Settings</h2>
<div class="row">
<body>
	<b>Slot Settings</b><br>
	You can edit the name of each sequence to anything of your choosing. This name will appear on the MonsterRemote homescreen.
	If the animation/scene/sequence is enabled, a button will appear on the MonsterRemote home screen. 
	If animation/scene/sequence is disabled, it removes the button from the home screen. 
	Setting animation/scene/sequence to enabled or disabled will change the configuration of the MonsterShield for that particular slot.
		
</body>
</div>
<p></p>

	<div class="row">
  			<div style="float:left;margin-right:10px;margin-bottom: 10px;">
      			<%= button_to "Refresh Animations", :refresh_scenes, class: "btn btn-lg btn-default", data: { confirm: " Ambient mode must be turned off to continue. \n Please verify Ambient Mode is turned OFF! \n \n \n Are you sure you want to continue?"}, :method => :post %> 
     		</div>
	</div>
<div class="row">
<table>
	
  <thead>
    <tr>
      <th style="padding-right: 15px">Sequence</th>
      <th style="padding-right: 15px">Name</th>
      <th style="padding-right: 15px">Enabled</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @scenes.each do |scene| %>
      <tr>
      <% @scenenumber = scene.id - 1 %>	 
      <% if (scene.id - 1).to_s == "10"
        @scenenumber = "A"
     elsif (scene.id - 1).to_s == "11"
        @scenenumber = "B"
     elsif (scene.id - 1).to_s == "12"
        @scenenumber = "C"
     elsif (scene.id - 1).to_s == "13"
        @scenenumber = "D"
     elsif (scene.id - 1).to_s == "14"
        @scenenumber = "E"
    end %>
      	<td style="padding-right: 15px"><%= @scenenumber %></td>
        <td style="padding-right: 15px"><%= scene.name %></td>
        
        <td style="padding-right: 15px"><%= scene.enabled %></td>
        <td><%#= link_to 'Show', scene %></td>
        <td><%= link_to 'Edit', edit_scene_path(scene) %></td>
        <td><%#= link_to 'Destroy', scene, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>		
<br>
<br>
<div class="row">
<body><b>Ambient Settings</b><br>
	Toggle (Start/Stop) Ambient Mode on Slot 0</body>
</div>
	<p>	
		<div class="row">
  			<div style="float:left;margin-right:10px;margin-bottom: 10px;">
      			<%= button_to "STOP", :stop_ambient, class: "btn btn-lg btn-danger", data: { confirm: "STOP Ambient loop \n\nAre you sure?"}, :method => :post %> 
     		</div>
     	
  			<div style="float:left;margin-right:10px;margin-bottom: 10px;">
      			<%= button_to "START", :start_ambient, class: "btn btn-lg btn-success", data: { confirm: "START Ambient loop \n\nAre you sure?"}, :method => :post %> 
     		</div>
     	</div>	
  	</p>
<br>

<div class="row">
<body><b>Playback Settings</b><br>
	Change Slot Playback Mode. How will the next slot be selected?</body>
</div>
	<p>	
		<div class="row">
  			<div style="float:left;margin-right:10px;margin-bottom: 10px;">
      			<%= button_to "Sequential", :sequential_mode, class: "btn btn-lg btn-info", data: { confirm: "Use 'Sequential'\n\nAre you sure?"}, :method => :post %> 
     		</div>
     	
  			<div style="float:left;margin-right:10px;margin-bottom: 10px;">
      			<%= button_to "Random", :random_mode, class: "btn btn-lg btn-info", data: { confirm: "Use 'Random'\n\nAre you sure?"}, :method => :post %> 
     		</div>
     		
     		<div style="float:left;margin-right:10px;margin-bottom: 10px;">
      			<%= button_to "Single", :single_mode, class: "btn btn-lg btn-info", :method => :get %> 
     		</div>
     	</div>	
  	</p>
<br>

<%#= link_to 'New Scene', new_scene_path %>
</html>
